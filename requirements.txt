# Dependencias del Proyecto: Reporte Economía Naranja SENA
# Instalación: pip install -r requirements.txt

# Procesamiento de datos
pandas>=2.0.0

# Lectura/escritura de archivos Excel
openpyxl>=3.1.0      # Para archivos .xlsx
pyxlsb>=1.0.10       # Para archivos .xlsb (formato binario)

# Base de datos (sqlite3 viene con Python 3.10+, no requiere instalación)

# Compilación a ejecutable (solo necesario para desarrollo)
pyinstaller>=6.0.0

# Opcional: Barra de progreso para procesos largos
tqdm>=4.65.0